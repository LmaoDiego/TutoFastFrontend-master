import { EconomicLevelDataSource } from './economic-level-data-source';

describe('EconomicLevel', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new EconomicLevelDataSource()).toBeTruthy();
  });
});
