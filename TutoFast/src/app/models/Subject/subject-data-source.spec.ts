import { SubjectDataSource } from './subject-data-source';

describe('SubjectDataSource', () => {
  it('should create an instance', () => {
    expect(new SubjectDataSource()).toBeTruthy();
  });
});
